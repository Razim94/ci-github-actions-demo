name: Midnight Daily Scheduled Build

on:
  schedule:
    - cron: "0 0 * * *"   # every day at 00:00 UTC
  workflow_dispatch:      

jobs:
  daily:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Print success message
        run: echo "Scheduled build completed successfully!"
